---
name: scaffold_project
description: 基于目标语言的最佳实践初始化新的项目结构。
---

**步骤:**
1. **分析需求:** 确定语言、框架和类型 (CLI, Web, Lib)。
2. **目录结构:** 创建标准文件夹 (`src`, `tests`, `docs`, `config`, `scripts`)。
3. **配置初始化:** 创建 `.gitignore`, `.editorconfig`, `README.md`, `LICENSE`。
4. **依赖设置:** 初始化包管理器 (`Cargo.toml`, `go.mod`, `package.json`, `pyproject.toml`)。
5. **CI/CD:** 创建基本的 GitHub Actions 工作流（构建和测试）。
