---
name: scaffold_project
description: Initialize a new project structure based on best practices for the target language.
---

**Steps:**
1. **Analyze Requirements:** Determine language, framework, and type (CLI, Web, Lib).
2. **Directory Structure:** Create standard folders (`src`, `tests`, `docs`, `config`, `scripts`).
3. **Config Initialization:** Create `.gitignore`, `.editorconfig`, `README.md`, `LICENSE`.
4. **Dependency Setup:** Initialize package manager (`Cargo.toml`, `go.mod`, `package.json`, `pyproject.toml`).
5. **CI/CD:** Create basic GitHub Actions workflow (Build & Test).
